<?php

namespace 

public function up()
{
  Schema::create('materias', function (Blueprint $table))
  {
    $table->increments('id');
    $table->integer('user_id')->unsigned;
    $table->increments('id');
    $table->increments('id');
    $table->increments('id');
    $table->increments('id');
  }
}
